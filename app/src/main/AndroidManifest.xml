<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.example.sociallearn">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SocialLearn">
        <activity android:name="br.com.fiap.sociallearn.view.cadastro.CadastroCompartilharSobre" />
        <activity android:name="br.com.fiap.sociallearn.view.cadastro.CadastroAprenderSobre" />
        <activity android:name="br.com.fiap.sociallearn.view.cadastro.CadastroPerfil" />
        <activity android:name="br.com.fiap.sociallearn.view.cadastro.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>